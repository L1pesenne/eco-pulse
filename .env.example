# Optional. Without this value, PULSE uses its local adaptive environmental guide.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional override for the SQLite file.
DATABASE_PATH=./data/eco-pulse.sqlite
